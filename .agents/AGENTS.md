# Reglas del Proyecto

- No utilizar la herramienta `browser_subagent` para generar videos o capturas de pantalla de pruebas de QA de forma predeterminada, a menos que sea explícitamente solicitado por el usuario, para evitar retrasos en el ciclo de respuesta y agilizar el desarrollo.
